# Copy to .env and adjust. Used by Makefile and docker-compose.
# Host port to publish the app on. Use a different port if other Dockerized sites use 3000.
PORT=3000
# Container name (must be unique if you run multiple stacks on the same host).
CONTAINER_NAME=tinytools-app
